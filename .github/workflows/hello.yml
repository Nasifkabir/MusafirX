name: My First Workflow

on:
  push:
    branches: [ main ]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print a message
        run: echo "Yaaa ! Action Workflow Running accurately!"

      - name: Show current date and time
        run: date

      - name: show which folder I am in
        run: pwd
        
